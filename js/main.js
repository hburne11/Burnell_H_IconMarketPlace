

  let stick = document.querySelector("#stick");

  function logMyId() {
  console.log(this.id);
  }

  stick.addEventListener("click", logMyId);

  let sticks = document.querySelector("#sticks");

  function logMyId() {
  console.log(this.id);
  }

  sticks.addEventListener("click", logMyId);

  let skater = document.querySelector("#skater");

  function logMyId() {
  console.log(this.id);
  }

  skater.addEventListener("click", logMyId);

  let skate = document.querySelector("#skate");

  function logMyId() {
  console.log(this.id);
  }

  skate.addEventListener("click", logMyId);

  let rink = document.querySelector("#rink");

  function logMyId() {
  console.log(this.id);
  }

  rink.addEventListener("click", logMyId);

  let puck = document.querySelector("#puck");

  function logMyId() {
  console.log(this.id);
  }

  puck.addEventListener("click", logMyId);

  let player = document.querySelector("#player");

  function logMyId() {
  console.log(this.id);
  }

  player.addEventListener("click", logMyId);

  let pads = document.querySelector("#pads");

  function logMyId() {
  console.log(this.id);
  }

  pads.addEventListener("click", logMyId);

  let net = document.querySelector("#net");

  function logMyId() {
  console.log(this.id);
  }

  net.addEventListener("click", logMyId);

  let goalie = document.querySelector("#goalie");

  function logMyId() {
  console.log(this.id);
  }

  goalie.addEventListener("click", logMyId);
